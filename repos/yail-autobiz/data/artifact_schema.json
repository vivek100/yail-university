{
  "type": "object",
  "required": ["workflow_name", "labels", "rules"],
  "properties": {
    "workflow_name": {"type": "string"},
    "labels": {"type": "array", "items": {"type": "string"}, "minItems": 1},
    "rules": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["label", "keywords"],
        "properties": {
          "label": {"type": "string"},
          "keywords": {"type": "array", "items": {"type": "string"}}
        }
      }
    }
  }
}
